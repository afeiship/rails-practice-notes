# install ruby:

## install or set default:
```shell
rvm install 2.2.0
rvm list
rvm use 2.2.0 --default
```

## check if:
```shell
rvm list
rvm rubies

# =* ruby-2.2.0 [ x86_64 ]
# => - current
# =* - current && default
#  * - default
```